import 'package:freelancer/screen/widgets/auth/unified_log_in.dart';

/// Alias for [UnifiedLogIn] — same screen for all users.
class ClientLogIn extends UnifiedLogIn {
  const ClientLogIn({super.key});
}
